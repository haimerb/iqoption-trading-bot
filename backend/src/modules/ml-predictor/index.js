'use strict';

const AIPredictionModule = require('./aiPredictionModule');
const MLPredictor = require('./mlPredictor');
const PriceActionPredictor = require('./priceActionPredictor');

module.exports = {
  AIPredictionModule,
  MLPredictor,
  PriceActionPredictor
};